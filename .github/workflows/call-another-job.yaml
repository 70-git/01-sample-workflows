name: sample-workflow
on:
  workflow_dispatch:

jobs:
  feature-pipeline:
    uses: 70-git/common-workflows/.github/workflows/test-workflow.yml@main
    with:
      environment: dev
      project: roboshop
      component: frontend
