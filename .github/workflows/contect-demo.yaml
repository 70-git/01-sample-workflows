name: context-demo
on:
  push:
jobs:
  context-demo:
    runs-on: ubuntu-latest
    steps:
      - name: "print info"
        run: |
          echo '${{ toJSON(github) }}'
          echo "branch_name: ${{github.ref_name}}"

