name: feature-pipeline
on:  [push]

jobs:
  feature-pipeline:
    uses: 70-git/common-workflows/.github/workflows/nodejs-featurepipeline.yaml@main
    with:
      environment: dev
      project: roboshop
      component: frontend
